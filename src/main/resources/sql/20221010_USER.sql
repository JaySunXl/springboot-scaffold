CREATE TABLE `scaffold`.`user`  (
                                        `user_id` int(20) NOT NULL AUTO_INCREMENT COMMENT '用户id',
                                        `user_name` varchar(50) NOT NULL COMMENT '用户名称',
                                        PRIMARY KEY (`user_id`)
);